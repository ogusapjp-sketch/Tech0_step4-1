# 管理用（DDL・ユーザー管理）の接続情報のひな形。アプリのコンテナには渡さない（最小権限。design.md 7.5）
# 使い方：cp .env.azure.admin.example .env.azure.admin して値を埋める。コミットしない（.gitignore）
#   set -a; . ./.env.azure; . ./.env.azure.admin; set +a
# 値は単引用符で囲む（.env.azure と同じ書き方）

ADMIN_DB_USER='tech0'
ADMIN_DB_PASSWORD='ここに共有サーバのパスワードを貼る'
